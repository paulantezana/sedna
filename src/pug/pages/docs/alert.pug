extends ../../config/template
append config
  -title = "Alerts"
  -description = "Use Sedna custom button styles for actions in forms, dialogs, and more with support for multiple sizes, states, and more."
  -ogUrl = "/index.html"
  -template = 2
  
block main
  main
    h1= title
    p= description
    .SnCard.SnMb-16
      section.SnCard-body
        h2.SnCard-title Basic
        //- p.SnCard-description Hay un botón <code>Default</code>, un botón <code>Primary</code>, 
        //-   | un botón <code>Success</code>, un botón <code>Warning</code>,
        //-   | un botón <code>Error</code>, un botón <code>White</code> en sedna.
        .DocExample
          .SnAlert message in alert
          .SnAlert.success message in alert
          .SnAlert.info message in alert
          .SnAlert.warning message in alert
          .SnAlert.error message in alert
        pre.language-html
            //- code= '<div class="SnBtn">Default</div>' + ' \n<div class="SnBtn primary">Primary</div> \n<div class="SnBtn success">Success</div> \n<div class="SnBtn warning">Warning</div> \n<div class="SnBtn error">Error</div> \n<div class="SnBtn white">White</div>'