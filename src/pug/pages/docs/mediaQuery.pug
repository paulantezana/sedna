extends ../../config/template
append config
    -title = "Media Query"
    -description = "Media query."
    -ogUrl = "/index.html"
    -template = 2
block main
    section.SnMb-16
        h2 Only SCSS
        pre.language-scss
            code.
                @include from(l){
                    // css styles
                }
                @include to(l){
                    // css styles
                }