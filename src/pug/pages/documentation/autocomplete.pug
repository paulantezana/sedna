extends ../../config/template
append config
    -title = "Autocomplete"
    -description = "Autocomplete"
    -ogUrl = "/index.html"
    -template = 2

block main
    .SnGrid.l-2
        +codeBox(
            '', 
            '', 
            ''
        )
