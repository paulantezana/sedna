extends ../../config/template
append config
    -title = "Contain"
    -description = "Contain."
    -ogUrl = "/index.html"
    -template = 2
block main
    .SnGrid.l-2
        +codeBox(
            'Basic', 
            '', 
            '<div class="SnContainer">\n</div>'
        )